# Вариант 6: Анализ отказоустойчивости корпоративной сети

Этот проект реализует программный модуль для анализа топологии сети на отказоустойчивость.

## 📋 Что реализовано (Задание)

В рамках варианта 6 были созданы агенты, выполняющие поиск ключевых уязвимостей в графе компьютерной сети:

1.  **Поиск точек сочленения** (`action_find_articulation_points`) — узлы (устройства), выход которых из строя разделит сеть на несколько изолированных частей.
2.  **Поиск мостов** (`action_find_bridges`) — связи (кабели), разрыв которых приведет к потере связи между частями сети.
3.  **Определение вершинной связности** (`action_find_vertex_connectivity`) — проверка, является ли граф сети связным (можно ли добраться от любого устройства к любому другому).

В базе знаний также создана **тестовая сеть** ООО "ИнфоТех" (`network_infotech`), состоящая из серверов, рабочих станций, коммутаторов и роутеров.

---

## 🚀 Как запустить

Для запуска используется Docker. Убедитесь, что Docker установлен и запущен.

### 1. Запуск системы
Находясь в корневой папке проекта (`PpoisNetworkResilience`), выполните команду:
```bash
docker compose up -d
```
Эта команда соберет проект, соберет базу знаний (с нашими дополнениями) и запустит сервер.

### 2. Остановка системы
Чтобы остановить работу:
```bash
docker compose down
```

---

## 🧪 Как проверить работу

### Способ 1: Через веб-интерфейс (SC-Web)

1.  Откройте в браузере: **[http://localhost:8000](http://localhost:8000)**
2.  В строке поиска введите: **`network_infotech`**
3.  Нажмите на найденный элемент, чтобы перейти в режим просмотра.
4.  В панели действий (или через контекстное меню) выберите одну из команд:
    *   **Найти точки сочленения**
    *   **Найти мосты**
    *   **Найти вершинную связность**
5.  Результат выполнения команды отобразится графически (подсвеченные узлы/связи) или в виде сообщения.

### Способ 2: Через командную строку (Модульные тесты)

Вы можете запустить автоматические тесты, которые проверяют корректность работы алгоритмов на простых примерах (линейный граф).

Выполните следующую команду в терминале (когда система запущена):

```bash
docker compose exec machine env LD_LIBRARY_PATH=/example-app/install/sc-machine/lib:/example-app/build/Release/extensions /example-app/build/Release/extensions/network_resilience_tests
```

Если всё работает правильно, вы увидите вывод:
```
[  PASSED  ] 3 tests.
```

---

## 📂 Структура файлов

*   `problem-solver/cxx/network_resilience_module/` — Исходный код модуля (C++).
    *   `agents/` — Реализация алгоритмов (DFS/BFS).
    *   `keynodes/` — Определение ключевых узлов.
    *   `test/` — Код тестов.
*   `knowledge-base/network-domain/` — Описание предметной области и тестовой сети (SCs).
